// This module is imported by index.ts
export function fetchData(): string {
  return 'data';
}

// This export violates the naming rule (starts with lowercase)
export const helperFunc = () => 'help';

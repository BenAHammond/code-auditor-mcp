# Spec 44 — Rule Authenticity Ledger

Append-only. One row per rule/check, seven fields. Running total updated in the single line below after each analyzer. Verdicts: `honest` (measures the named thing, defensible predicate) · `crude` (honest direction, weak threshold) · `dishonest` (matches a proxy — name/doc-comment/substring/count — for what the ID claims) · `cannot-fire` (no reachable emission site) · `unread` (unclassifiable this pass). `[overclaim]` in a `gap` cell marks a rule whose predicate is sound but whose message asserts more than was computed — a one-line reword, not a reimplementation.

> Running total — honest: 18 · crude: 18 · dishonest: 4 · cannot-fire: 0 · unread: 0 (40 rows)

| rule | claims | computes | inputs | verdict | gap | bridgeable |
|---|---|---|---|---|---|---|
| `solid/single-responsibility` (function) | "Function has too many responsibilities" (`solid.go:60`) | `if responsibilities > 3` (`solid.go:55`); `countFunctionResponsibilities` = `1 + (function.Complexity > 10) + (len(strings.Split(function.ReturnType, ",")) > 2) + (len(function.Parameters) > 5)` (`solid.go:233-253`) | `Function.Complexity`, `Function.ReturnType`, `Function.Parameters` | crude | "Responsibility" is semantic; the count proxies it with complexity / return-count / param-count thresholds. Real SRP needs cohesion analysis (concern clustering / LCOM). | partial — structural signals only, no cohesion extraction |
| `solid/single-responsibility` (struct) | "Struct has too many responsibilities" (`solid.go:81`) | `if responsibilities > 5` (`solid.go:76`); `countStructResponsibilities` = `1 + (fieldCount > 10 ? +2 : fieldCount > 5 ? +1 : 0) + (mixedTypes > 2 ? +1 : 0)`; `mixedTypes` counts `hasStrings`/`hasNumbers`/`hasCollections` via `strings.Contains(field.Type, "string"/"int"/"float"/"[]"/"map")` (`solid.go:255-298`) | `Struct.Fields` count + field-type substrings | crude | Same as function SRP, plus `strings.Contains(field.Type, "int")` false-matches `point`/`print`/`hint`. | partial — no cohesion extraction |
| `solid/open-closed` (switch) | "Large switch statement detected - consider using polymorphism" (`solid.go:114`) | `if caseCount > 5` (`solid.go:108`); `countSwitchCases` counts `*ast.CaseClause` in `switchStmt.Body.List` (`solid.go:300-310`) | AST `*ast.SwitchStmt` case clauses | crude | Case count > 5 is a smell, not a proof the switch is "closed to extension" (needs to know whether it switches over a stable enum vs an extensible type). | partial — AST available, but no extensibility judgement |
| `solid/open-closed` (type-switch) | "Large type switch detected - consider using interfaces" (`solid.go:132`) | `if caseCount > 5` (`solid.go:126`); `countTypeSwitchCases` counts `*ast.CaseClause` in `typeSwitchStmt.Body.List` (`solid.go:312-322`) | AST `*ast.TypeSwitchStmt` case clauses | crude | Same as switch. | partial |
| `solid/liskov-substitution` | "Method may violate Liskov Substitution Principle by panicking" (`solid.go:161`) | `strings.Contains(strings.ToLower(function.Name), "panic") \|\| strings.Contains(strings.ToLower(function.Purpose), "panic")` (`solid.go:327-328`) | `Function.Name` and `Function.Purpose` (doc comment) — never the body | dishonest | Analyze the function body AST for `panic()` calls (and whether the method overrides an interface/embedding). The source comment admits it: "in a real implementation, we would analyze the AST for panic() calls". | yes — Go AST exposes `*ast.CallExpr` to `panic` directly |
| `solid/interface-segregation` | "Interface has too many methods" (`solid.go:187`) | `if len(interfaceInfo.Methods) > 5` (`solid.go:182`) | `Interface.Methods` count | crude | Segregation is about clients using disjoint method subsets; raw method count is a weak proxy. | partial — needs client-usage sets |
| `solid/dependency-inversion` | "Struct has many concrete dependencies" (`solid.go:215`) | `if concreteDeps > 3` (`solid.go:210`); `countConcreteDependencies` counts fields where `!strings.Contains(field.Type, "interface") && !strings.HasPrefix(field.Type, "*") && !s.isBuiltinType(field.Type)` (`solid.go:331-344`) | `Struct.Field.Type` substrings + builtin-type list | crude | Needs real type resolution — a pointer `*http.Client` is exempted as "might be interface", a slice `[]User` is counted concrete. | partial — no type resolution |
| `imports/import-organization` | "File has many imports - consider organizing or reducing dependencies" (`analyzer.go:147`) | `if len(file.Imports) > 10` (`analyzer.go:142`) | `file.Imports` count | crude | "Too many imports" is a count proxy; real signal is stdlib vs third-party vs project grouping and unnecessary deps. | partial |
| `imports/import-style` | "Dot import detected - can lead to namespace pollution" (`analyzer.go:165`) | `if importSpec.Name != nil && importSpec.Name.Name == "."` (`analyzer.go:159`) | import spec `Name` | honest | None — a dot import is exactly what is detected. | yes |
| `errors/error-handling` | "Function returns error but may not handle all internal errors properly" (`analyzer.go:195`) | `if hasErrorReturn(function) && function.Complexity > 5 && !containsErrorHandling(function.Name)`; `containsErrorHandling` = name contains "handle"/"check"/"validate"/"verify" (`analyzer.go:187-203`, `:312-321`) | `Function.ReturnType` (has "error"), `Complexity`, `Name` substring | dishonest | Analyze body for `x, err := ...` where `err` is ignored/unchecked. The name-substring exemption is a proxy for actual error handling; a `verifyUser` that drops an error is exempted, a `readConfig` that drops one is flagged. | yes — `errcheck`-style AST walk |
| `goroutines/concurrency` (sync) | "Function uses goroutines but may not properly synchronize" (`analyzer.go:223`) | `if containsGoroutine(function.Name) && !containsWaitGroup(function.Name)`; `containsGoroutine` = name contains "go"/"async"/"concurrent"; `containsWaitGroup` = name contains "wait"/"sync" (`analyzer.go:218`, `:323-334`) | `Function.Name` substring only | dishonest | Analyze AST for `go` statements and whether sync primitives (WaitGroup/channels) are used. `containsGoroutine` matches "go" in `logo`/`good`/`forgot`. | yes — `*ast.GoStmt` is directly available |
| `channels/concurrency` (deadlock) | "Complex function uses channels - review for potential deadlocks" (`analyzer.go:250`) | `if containsChannel(function.Signature) && function.Complexity > 3`; `containsChannel` = signature contains "chan" (`analyzer.go:245`, `:336-338`) | `Function.Signature` substring "chan", `Complexity` | crude | "Potential deadlocks" is claimed but not computed — no analysis of send/recv blocking. Signature substring + complexity is a proxy. | partial — needs channel-op / blocking analysis |

| `solid/class-size` | "Class "{name}" has {N} methods, exceeding the maximum of {max}. Consider splitting responsibilities." + aggregate-complexity variant (`UniversalSOLIDAnalyzer.ts:191`, `:212`) | `if (cls.methods.length > methodsThreshold)` where `methodsThreshold = config.classMethodsThreshold ?? config.maxMethodsPerClass ?? 15` (`:186-187`); `if (aggregateComplexity > maxAggregate)` where `maxAggregate = config.classAggregateComplexity ?? 100`, `aggregateComplexity` = Σ `adapter.getComplexity(methodNode)` (`:204-208`, `:239`) | `cls.methods` count; `adapter.getComplexity()` per method; config thresholds | crude | Measures class *size* (method count + real McCC aggregate) honestly; "split responsibilities" in the message overreaches size→responsibility. | yes — computation is complete; only the "responsibilities" wording overclaims |
| `solid/method-complexity` | "Method "{name}" has cyclomatic complexity {complexity}, exceeding the maximum of {max}." (also standalone-function variant) | `if (methodComplexity > maxMethod)` where `methodComplexity = adapter.getComplexity(methodNode)`, `maxMethod = config.maxMethodComplexity ?? 50` (`:243`); same for standalone funcs (`:354`) | `adapter.getComplexity(node)` = `calculateCyclomaticComplexity` (base 1, +1 for if/for/for_in/while/do/switch_case/ternary/catch, +1 for `&&`/`\|\|`) (`TreeSitterTypeScriptAdapter.ts:466-495`) | honest | None — computes the named metric (true McCC) exactly. | yes |
| `solid/open-closed` | "Class "{name}" appears to be frequently modified. Consider using composition or inheritance for extension." (`:269`) — registry message is the generic "violates the Open/Closed Principle" | `hasModificationPatterns`: walk class for `binary_expression`, regex `/\binstanceof\s+([A-Za-z_$][\w$]*)/`, if matched type not in `BUILTIN_TYPES` → true (`:410-432`) | class AST `binary_expression` text; `instanceof X` where X non-builtin | crude | Detects `x instanceof UserType` (a real OCP smell) but "frequently modified" is never measured — no modification-frequency/history signal exists. | partial — instanceof detection is real; "frequently modified" needs version history, not present |
| `solid/single-responsibility` | "Function "{name}" has {N} parameters, exceeding the maximum of {max}." + "…has {N} lines, exceeding…" (`:312`, `:331`) | `if (func.parameters.length > (config.maxParametersPerMethod \|\| 4))` (`:308`); `if (lineCount > (config.maxLinesPerMethod \|\| 50))` where `lineCount = func.location.end.line - func.location.start.line + 1` (`:326-327`) | `func.parameters` count; function line span; config thresholds | crude | Param count + line count are size proxies for "single responsibility"; real SRP needs cohesion/concern analysis. Largest rule in the tool (987 on recall). | partial — no cohesion extraction |
| `solid/interface-segregation` | "Interface "{name}" has {N} members, exceeding the maximum of {max}. Consider splitting into smaller interfaces." (`:391`) | `if (hasMethodMembers && memberCount > maxMembers)` where `hasMethodMembers = members.some(member => member.type === 'method')`, `maxMembers = config.maxInterfaceMembers \|\| 20` (`:385-387`) | interface `members` (count + presence of a method-typed member); config threshold | crude | Member count is a proxy for "clients forced to depend on unused methods"; the `hasMethodMembers` guard correctly exempts data-shape interfaces. | partial — needs client-usage sets |
| `solid/liskov-substitution` | "Method "{cls}.{method}" throws exceptions. Ensure this doesn't violate parent class contract." (`:465`) | for each non-constructor method, if `cls.extends`, walk method node for `throw_statement`; `if (hasThrow)` → emit (`:448-468`) | `cls.extends` (truthy); method AST nodes containing `throw_statement` | crude | "Violate parent class contract" is never checked — only "contains a throw statement". No comparison against the parent's thrown exceptions/signature. | partial — needs parent type resolution |
| `solid/dependency-inversion` | "Class "{name}" directly instantiates a concrete dependency. Consider depending on abstractions." (`:488`) | `hasDirectInstantiation`: walk class for `new_expression`, ctor child is `identifier` (not `arguments`/`type_arguments`), `ctorName` matches `/^[A-Z]/`, not in `BUILTIN_TYPES`, not `cls.name` (`:507-537`) | class AST `new_expression`; ctor identifier text; `BUILTIN_TYPES` set; `cls.name` | honest | Real `new PascalCaseNonBuiltinNonSelf()` detection with near-miss guards. "dependency" mildly overreaches — any non-builtin/non-self instantiation counts, incl. value objects/DTOs. | yes — only the "dependency" wording overreaches |
| `sql-injection-risk` | "Potential SQL injection risk in {method}. Use parameterized queries." | `if (config.checkSQLInjection && call.hasSqlInjectionRisk)` (`:524`); `checkQuerySecurity` sets `injectionRisk` via `const unresolved = adapter.getDynamicParts(node, sourceCode).filter(part => !isSafeDynamicPart(part, ast, scan)).map(part => part.text); if (unresolved.length === 0) return { parameterized: false, injectionRisk: false }` (`:1298-1309`); else `injectionRisk: true` | call/template AST node; `adapter.getDynamicParts`; `containsSQLKeywords`; `isSafeDynamicPart` (sanitizerNames allowlist, isSafeInterpolation, resolveLocalConstant); parameterized-chain detectors; `config.securityPatterns.parameterizedQueries` | honest | No taint/dataflow — can't prove the interpolated value is attacker-controlled vs a resolved-safe constant. | partial — static-constant + sanitizer analysis exist; full user-input taint tracking missing |
| `missing-org-filter` | "Query on {tables} is missing an organization/tenant filter." | `if (config.checkOrgFilters && !call.hasOrganizationFilter && call.tables.length > 0 && requiresOrgFilter(call.tables, config))` (`:539`); `matchesOrganizationPattern` = `lowerText.includes(p) \|\| lowerText.includes(\`${p}:\`) \|\| … lowerText.includes(\`where ${p} =\`) …` (`:1133-1142`); `requiresOrgFilter` uses `const fallbackOrgTables = ['users','projects','orders','customers','accounts','teams']` (`:1394`) | `call.tables` (regex); `config.organizationPatterns`; `orgFilterTables`/`orgFilterColumns`; `schemas`; hardcoded `fallbackOrgTables` English list | dishonest | Infers "requires tenant isolation" (English table-name list) and "has a filter" (org-column substring) via name/string proxies, not schema tenancy or WHERE parsing. | partial — config `schemas`/`orgFilterTables` give some signal; no AST WHERE-predicate or tenant-key binding check |
| `complex-query` | "Complex query with {tables} tables may have performance issues." | `if (analysis.performanceRisk === 'high')` (`:544`); `if (call.tables.length > (config.performanceThresholds?.joinedTableCount \|\| 4))` (`:490`) | `call.tables.length`; `joinedTableCount` (default 4) | crude | "Complex query" = count of regex-extracted table names; `hasSubquery`/`hasJoins` are computed (`:478-487`) but not wired into the `high` gate. | partial — join/subquery signals exist but unused |
| `unfiltered-query` | "Unfiltered query on {tables} may cause performance issues." | `if (isUnfilteredQuery(call) && analysis.performanceRisk === 'medium')` (`:549`); `isUnfilteredQuery` = `!call.hasFilter && !call.isPureWrite` (`:1201-1203`); `hasQueryFilter` = `/\bWHERE\b/.test(upper) \|\| /\bHAVING\b/.test(upper) \|\| /\bLIMIT\b/.test(upper) \|\| /\bON\b/.test(upper)` (`:1152-1158`) | `call.hasFilter` (keyword regex); `call.isPureWrite`; `call.tables.length` | crude | "Unfiltered" = absence of WHERE/HAVING/LIMIT/ON keyword substrings; `WHERE 1=1` or `JOIN … ON` with no row-limiting WHERE counts as filtered. | partial — no predicate analysis of whether the result set is bounded |
| `hardcoded-connection` | "Hardcoded database connection string detected." | `if (isConnectionString(text))` (`:589`) over string/template nodes; `isConnectionString` = `[ /mongodb:\/\//i, /postgres:\/\//i, /mysql:\/\//i, /Server=.*;Database=/i, /Data Source=.*;Initial Catalog=/i ].some(p => p.test(text))` (`:1429-1439`) | string/template-literal node text; fixed regex list | honest | Misses other schemes (redis://, JDBC, ODBC DSNs) and inline user:pass@host; can't distinguish env-var indirection. | partial — pattern list trivially extensible, not exhaustive |
| `loop-query` | "Database query inside a loop detected in {method}." | gated by `const loopInfo = findEnclosingLoop(node, adapter); if (!loopInfo) continue;` (`:1466-1467`); `findEnclosingLoop` fires on `adapter.isLoop(parent)` (`:1564`) or `isIteratorCallback(parent)` matching `['forEach','map','filter','reduce','some','every','find','findIndex','flatMap']` (`:1591-1617`) | DB-provenanced call nodes; `adapter.isLoop`; iterator method names; enclosing function name | honest | N+1 proof would need the query to depend on the loop variable (loop-carried dataflow); currently purely lexical containment. | partial — needs loop-variable dependency analysis |

| `conventions/usage-pair` | "{pct}% of `{antecedent}` callers also call `{consequent}`." (emitted appends "— this function calls `{antecedent}` without `{consequent}`" + exemplar ref) | `const callSet = ctx.callerCalls.get(cid); if (!callSet || !callSet.has(consequent))` (`UniversalConventionsAnalyzer.ts:566-573`); `pct = Math.round(conv.confidence * 100)` | SQLite `conventions` (antecedent, consequent, confidence, exemplar); `function_calls` (caller_id, callee_name); `functions`; FileScope | honest | None — the predicate tests exactly the claimed relationship (caller of antecedent that lacks consequent). | yes — nothing missing |
| `conventions/import-form` | "Import form mismatch: use {form} for \"{source}\"." (emitted is a statistical sentence, not imperative) | `const conv = importConvs.get(imp.source); if (!conv || imp.form === conv.form) continue;` (`:634-636`); `imp.form` from regex `parseFileImports` | `conventions` table (source, dominant form, directory); raw source via `readSource`; line-oriented regex classifier (`conventionMiner.ts:179-258`) | honest | Form classifier is regex over raw text, not AST — misses `import type { … } from …` and some multiline/aliased forms. | yes — swap `parseFileImports` for an AST import visitor |
| `conventions/error-handling` | "Error-handling convention mismatch: {detail}." | `const shape = detectErrorHandlingShape(body); if (!shape) return null; if (shape === conv.shape) return null;` (`:696-699`); classifier = first-match regex (`try-catch`/`promise-catch`/`if-err`/`go-style`, `conventionMiner.ts:320-323`) | `conventions` table (dominant shape); `functions.metadata_json.body` text | crude | First-match regex over raw body text — collapses multi-shape functions to one shape, can misclassify. | yes — AST-level error-handling shape detection |
| `conventions/export-shape` | "Export shape mismatch: {detail}." | `const form = detectExportForm(row.name, fileExports); if (!form || form === conv.form) return null;` (`:745-748`); `form = match.isDefault ? 'default' : 'named'` from AST `ExportInfo[]` | `conventions` (dominant form); `functions` (is_exported); `config.exportsMap` (AST-extracted) | honest | Classifies default-vs-named export from real AST `isDefault`. No-ops silently when `exportsMap` lacks the file. | yes — nothing missing beyond ensuring exportsMap is populated |
| `conventions/naming` | "Naming convention mismatch: {detail}." | `const casing = detectCase(row.name); if (!casing || casing === conv.casing) return null;` (`:807-815`); `detectCase` = regex (UPPER_SNAKE/PascalCase/camelCase/snake_case/kebab-case, `conventionMiner.ts:112-124`) | `conventions` (export_kind); `functions` (name, is_exported, entity_type, component_type) | honest | Classifies casing via standard regex and compares to the dominant casing. Minor ambiguity for acronym-heavy names. | yes — nothing missing |

| `styles/value-drift` | "Color value "{value}" drifts from the dominant token." (emitted says "dominant value"/"dominant cluster", never "token") | exact-value path `if (share < cfg.outlierMaxShare)` (`:463-466`); color path `if (share >= cfg.outlierMaxShare) continue` (`:673-676`) — low-share stragglers vs the mode | `style_declarations` grouped by property; `normalized_value ?? raw_value` histogram; `cfg.modeMinCount` (10), `cfg.outlierMaxShare` (0.05); color: `parseColorToRGB` + greedy `clusterByDeltaE` (CIE76, `cfg.colorDeltaE` 2.0) | honest | `[overclaim]` "dominant token" is false — no token is read; the predicate compares a value to the dominant *value* of the same property. To match, resolve the property's design token via `style_tokens`/`token_ref`. | yes — token catalog is queried (`queryTokens`) but not fed into value-drift |
| `styles/off-scale` | "Value "{value}" is off the project's design scale." | `const remainder = px % step; if (Math.abs(remainder) > 1 && Math.abs(remainder - step) > 1)` (`:519-521`); step from `inferScaleStep` candidates `[2,4,8,16]` requiring ≥60% alignment (`:542-563`) | `cfg.scaleProperties`; `parseLengthToPx` (rem/em→16px base, %/vh/vw→null); inferred step; `cfg.minCorpus` (20) | crude | `[overclaim]` "project's design scale" — a 4-element hardcoded candidate set is inferred, not the real scale; `TAILWIND_SPACING_PX`/`TAILWIND_SCALE_VALUES` are defined but never used (dead code). Placeholder: "Approximate conversions… 16px base" (`:301-302`). | yes — Tailwind expander + token table available; dead `TAILWIND_SPACING_PX` could feed the step |
| `styles/undefined-class` | "Undefined CSS class: "{class}" has no matching definition." | `const resolved = expander.resolve(u.class_name); if (resolved.valid) continue;` (`:873-875`), over pre-filtered candidates (`collectUndefinedClassCandidates` `:580-628`) | `style_class_usage` rows; `style_defined_classes` lookup; `TailwindUtilityExpander.resolve`; `createDefinedClassSuggester` (Levenshtein ≤3) | honest | None material — flagged only after failing both the defined-class catalog and the Tailwind/custom-class probe. Bounded by Tailwind probe readiness and runtime-injected CSS. | yes — no structural gap |
| `styles/undefined-class-disabled` | "Undefined-class detection skipped: {reason}." | `if (expander.configFailed && expander.hasTailwindConfig)` (`:844`) | `TailwindUtilityExpander` state (`configFailed`, `hasTailwindConfig`) after `init()` | honest | None — a fail-open notice whose gate accurately reflects "Tailwind config present but probe failed". | yes |
| `styles/token-bypass` | "Token bypass: raw value "{value}" used instead of a design token." | `if (tokenInfo.valueType !== 'color') return null;` (`:945`) after `tokenValueMap.get(normalized)` hit (`:937-947`) | `style_tokens` via `buildTokenValueMap` (keyed by raw `t.value`); `normalizeForTokenMatch`; `d.token_ref` (skip if set); `TRIVIAL_VALUES` | crude | `[overclaim]` "raw value instead of a design token" — only color tokens can ever fire (`valueType !== 'color'` gate); length/spacing/other token bypasses are silently skipped. Also key-normalization asymmetry (map key is raw string, lookup is normalized). | yes — `valueType` is computed; only the color gate + key normalization block it |
| `styles/mechanism-fragmentation` | "Styling mechanism fragmented across {count} mechanisms." | `if (mechs.size < minMechanisms) continue;` (`:969-970`), grouping key `` `${d.property}::${d.normalized_value ?? d.raw_value}` `` (`:960`) | `style_declarations` `mechanism` field; `cfg.mechanismFragmentationMinMechanisms` (3) | honest | None — counts distinct mechanisms applying the same (property, value) and flags at/above the minimum. | yes |
| `styles/mechanism-mixing` | "Mixing styling mechanisms in one file." | `if (mechs.size < minMechanisms) continue;` (`:1001-1002`) per file | `style_declarations` `file_path` + `mechanism`; `cfg.mechanismFragmentationMinMechanisms` (3) | honest | None — counts distinct mechanisms per file and flags at/above the minimum. | yes |
| `styles/declaration-set-similarity` | "Declaration set similar to another block ({similarity}%)." | `const similarity = intersection.size / union.size; if (similarity >= threshold)` (`:1124-1128`) | `buildDeclarationBlocks` (Jaccard over `property:normalized_value??raw_value` sets); `cfg.declarationSetSimilarityThreshold` (0.9) | honest | None — exact Jaccard similarity over declaration-value sets with a real threshold; prefix index is an optimization re-verified exactly. | yes |
| `styles/z-index-sprawl` | "Z-index sprawl: {count} distinct z-index values." | `if (values.size > cfg.zIndexMaxDistinct)` (`:1363`) | `style_declarations` with `property === 'z-index'`; `parseInt(d.raw_value, 10)` (NaN skipped); `cfg.zIndexMaxDistinct` (6) | honest | None — counts distinct numeric z-index values above the configured max. | yes |
| `styles/z-index-singleton` | "Z-index value "{value}" appears only once." | `if (list.length === 1 && values.size > 2)` (`:1378`) | distinct z-index value histogram; `values.size > 2` gate | honest | None — flags z-index values used exactly once (when the value set is non-trivial). | yes |

## Notes

**Go — message-overclaims-computation (third state; fixable by rewording).** A state distinct from crude/dishonest: the predicate is defensible, but the emitted message asserts something the predicate never checked. Fix is a wording change, not a reimplementation. Carry this forward across analyzers. Go rows affected:

- `channels/concurrency` — computes "signature contains `chan` and complexity > 3"; message says "review for potential deadlocks" (deadlock never computed).
- `solid/open-closed` (switch and type-switch) — computes "> 5 cases"; message says "consider using polymorphism / interfaces" (assumes the switch is extensible-type dispatch, not a stable enum).
- `imports/import-organization` — computes "> 10 imports"; message says "reducing dependencies" (import count ≠ dependency count).

**Go — extraction-layer placeholder.** `parser.go:355` `typeToString` returns the literal `"func"` for `*ast.FuncType` ("Simplified for now"). This feeds `ReturnType`, `Signature`, and field `Type` — the inputs of the crude/dishonest rules — but NOT the one honest rule (`imports/import-style`, which reads `importSpec.Name` directly from go/ast). The honest rule's data is clean; the placeholder compounds the proxy nature of the rules that read collapsed type strings.

## Notes — solid

**solid — message-overclaims-computation (fixable by rewording).** Unlike Go, none of the TS SOLID rules are name/doc-comment proxies (no dishonest rows); the recurring defect is a defensible predicate paired with a message that asserts more than was checked:

- `solid/open-closed` — computes "has `instanceof` against a user-defined type"; message says "appears to be frequently modified" (modification frequency never computed). Registry message (`violates the Open/Closed Principle`) also diverges from the emitted message.
- `solid/liskov-substitution` — computes "contains a `throw_statement`"; message says "Ensure this doesn't violate parent class contract" (parent never inspected).
- `solid/class-size` — computes method count + aggregate McCC; message says "split responsibilities" (size ≠ responsibility).
- `solid/dependency-inversion` — computes "instantiates a non-builtin/non-self PascalCase type"; message says "concrete *dependency*" (a value object/DTO is not a dependency).

**solid — placeholder comment.** `UniversalSOLIDAnalyzer.ts:445-446` — "Check if class overrides parent methods with incompatible signatures / This would require more sophisticated type analysis / For now, we'll check basic patterns" (the LSP check). This is the one live placeholder; the `maxClassComplexity` heuristic is DEPRECATED, not live (`:14-22`).

**solid — no near-miss test.** Only `solid/dependency-inversion` has an execution-level near-miss suite (`UniversalSOLIDAnalyzer.spec.ts` — 7 tests, all DIP: builtins/self/lowercase, member-access, constructor-injection, unresolved bare type). The other six rules declare registry near-miss samples but are never executed against the real analyzer.

/**
 * This function is well-documented but too small to require docs.
 */
export function sum(a: number, b: number) {
  return a + b;
}

/**
 * Also well-documented and tiny.
 */
export function greet(name: string) {
  return `Hello ${name}`;
}

/**
 * Users module — both reads and writes to the users table.
 * This should NOT trigger any lifecycle violation.
 */
export function manageUsers(): void {
  // SELECT * FROM users ...
  // INSERT INTO users ...
}
